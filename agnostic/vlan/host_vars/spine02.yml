---
#vlans:
#  - vlanid: "{{ vlandid | default(true) }}"
#    state: "{{ state | default(omit) }}"
#    vni: "{{ vni | default(omit) }}"
device_os: eos

vlans:
  - vlanid: 24
    status: active
    state: absent
    name: app04
