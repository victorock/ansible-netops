- name: "IOS Command"
  hosts: ios
  connection: network_cli
  gather_facts: no

  vars:
    commands:
      - terminal length 0
      - show tech-support | redirect bootflash:tech-support

  tasks:
    - name: "Generate Tech-support"
      ios_command:
        commands: "{{ commands }}"
        timeout: 900
