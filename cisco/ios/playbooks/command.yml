- name: "IOS Command"
  hosts: ios
  connection: network_cli
  gather_facts: no

  vars:
    commands:
      - show version

  tasks:
    - name: "Ad-Hoc commands"
      ios_command:
        commands: "{{ commands }}"
