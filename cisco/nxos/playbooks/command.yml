- name: "NXOS Command"
  hosts: all
  connection: local
  gather_facts: no

  vars:
    commands:
      - show version

  tasks:
    - name: "Ad-Hoc commands"
      nxos_command:
        commands: "{{ commands }}"
