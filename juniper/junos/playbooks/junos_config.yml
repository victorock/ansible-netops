---
- name: "Configure Junos: hostname"
  hosts: all
  connection: network_cli
  gather_facts: no

  tasks:
    - name: Ensure state of configuration
      junos_config:
        lines: "{{ junos_config }}"
