---
- name: "Command Junos: run arbitrary commands"
  hosts: all
  connection: network_cli
  gather_facts: no

  tasks:
    - name: Ensure state of configuration
      junos_command:
        commands: "{{ junos_commands }}"
